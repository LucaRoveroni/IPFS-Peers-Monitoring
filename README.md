# IPFS-Peers-Monitoring
- Simple NodeJS script to monitor the reached IPFS peers and save them in a structured JSON file.
- Simple Python script to process the JSON file and produce some plot and charts.